# v1.1.0-rc.1 automated evidence

Implementation commit: `bbad04dbee4a46c0255a994f6cf5c564948c4ef8`

Passed on the exact clean implementation commit:

- provider/runtime contract qualification across integration, conformance, provisioning, gateway,
  reconciliation, and workflow packages;
- the complete race-enabled Go suite against fresh PostgreSQL migration `027`, including legacy
  deployment backfill, tenant isolation, immutable serving plans, and endpoint guard decisions;
- endpoint route compilation without data-path database reads, weighted/fallback policies,
  generation-safe stream pinning, and deleted-alias fail-closed behavior;
- authenticated API and CLI endpoint lifecycle, generated OpenAPI/Python/TypeScript clients, and
  endpoint identity in normalized request telemetry;
- live Docker Compose creation of a logical model, endpoint, lifecycle-managed binding, and first
  serving plan with synchronous route convergence; immediate inference succeeded, endpoint deletion
  returned 404, and the bound legacy deployment remained routable;
- deterministic endpoint Release Guard `REJECT` and `PASS` persistence with exact candidate-plan
  acceptance checks;
- full developer qualification including disposable Kind, container integration, stack smoke,
  failure recovery, production configuration, docs, dead-code, vulnerability, and release-package
  gates;
- Mintlify validation, link/anchor/snippet validation, and accessibility checks for 114 MDX files.

Commands:

- `make context`
- `make verify`
- `make docs-check`
- `make dev-check-full`
- `make qualify-contracts`
- `make qualify-local`

Durable local evidence:

- `.infercrane/dev-check/20260811T124343Z-25683`
- `.infercrane/contract-qualification/bbad04dbee4a46c0255a994f6cf5c564948c4ef8/qualification.json`
- `.infercrane/qualification/bbad04dbee4a46c0255a994f6cf5c564948c4ef8/qualification.json`

Real AWS, GCP, CoreWeave, RunPod, Kubernetes GPU, vLLM GPU, SGLang GPU, custom OCI GPU,
performance, pricing, cancellation, and provider cleanup evidence is **DEFERRED** until the single
consolidated manual qualification. No paid resource was created, no remote tag was pushed, and no
package was published.
