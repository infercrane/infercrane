# v1.0.0-rc.1 automated evidence

Implementation commit: `3c4cb6961640c471fbd12d79ec4d497f8da0ce9f`

Passed on the exact clean implementation commit:

- the complete Go race suite, generated API/SDK checks, Python and TypeScript package tests,
  Terraform protocol tests, GitHub action tests, dashboard tests, and repository invariants
- provider/runtime conformance, fault injection, acceptance approval locks, stale-evidence rejection,
  RunPod simulations, AWS CLI simulations, Kubernetes manifests, and disposable Kind lifecycle
- Docker build/integration, PostgreSQL migrations, stack smoke, restart/failure recovery, production
  Compose overlays, provider-neutral entrypoint behavior, and executable AWS CLI/kubectl image smoke
- every historical migration prefix through `026_request_quota_leases.sql`, concurrent startup,
  checksum tamper, gap, and newer-database fail-closed behavior
- distributed hard request-rate reservation, no-I/O gateway authorization, zero-policy revocation,
  pre-upstream 429 behavior, and concurrent PostgreSQL aggregate enforcement
- Mintlify validation, broken-link/anchor/snippet checks, accessibility checks for 111 MDX files,
  generated repository context, dead-code analysis, and release configuration validation
- `govulncheck` for the application and Terraform provider plus the TypeScript high-severity audit,
  with no known vulnerabilities reported by those tools
- exactly four `v1.0.0-rc.1` darwin/linux amd64/arm64 archives, checksums, four SPDX SBOMs,
  exact archive contents, native `version`/completion/help smoke, and generated Homebrew formula
- the separately gated stable `v1.0.0` artifact path on commit
  `44a327208c1344837ec0eed3a4ce3afb8d15806f`, without creating a source tag or publishing

Commands:

- `make context`
- `make verify`
- `make docs-check`
- `make dev-check-full`
- `make deadcode`
- `make audit`
- `make release-check`
- `make qualify-contracts`
- `make candidate-artifacts RELEASE_CANDIDATE_TAG=v1.0.0-rc.1`
- `make release-artifacts RELEASE_TAG=v1.0.0` (non-publishing handoff-path verification only)
- `make qualify-local`

Durable local evidence:

- `.infercrane/dev-check/20260811T113954Z-15158`
- `.infercrane/contract-qualification/3c4cb6961640c471fbd12d79ec4d497f8da0ce9f/qualification.json`
- `.infercrane/qualification/3c4cb6961640c471fbd12d79ec4d497f8da0ce9f/qualification.json`
- `dist/checksums.txt`
- `dist/*.tar.gz.sbom.json`
- `dist/homebrew/infercrane.rb`

Qualification review found and corrected two packaging defects before evidence was accepted: the
pre-tag GoReleaser lifecycle now uses an isolated local clone with a temporary tag, and formula
generation no longer mistakes its explanatory comment for an unresolved token. Review also changed
portable provider acceptance to use run-scoped deployment identities, compare provider inventory
against a pre-run baseline, and ignore stale markers from another commit.

Real RunPod elastic/serverless, AWS EC2 GPU, Kubernetes GPU, vLLM GPU, SGLang GPU, custom OCI GPU,
provider cancellation/deletion, benchmark, cost, and final resource-inventory evidence is
**DEFERRED** until the consolidated manual qualification. No paid resource was created and no real
provider, performance, pricing, or universal compatibility claim is marked passed.
