# v1.4.0-rc.1 automated evidence

Implementation commit: `0f581567ce1e4a9062b9e879afd0411043cfc30d`

Passed on the exact clean implementation commit:

- endpoint admission performs no database I/O and bounds concurrency, queue depth, wait time,
  request bytes, requested output tokens, priorities and buffered managed-capacity retries;
- streaming and governed external-capacity requests are never automatically replayed;
- protocol-native async requests require explicit encrypted-content consent and persist request and
  result bodies only as AES-256-GCM ciphertext bound to tenant/job identity;
- tenant-scoped idempotency, priority/deadline ordering, cancellation, expiry, bounded attempts,
  lease adoption and stale-worker fencing pass against PostgreSQL;
- HMAC-signed completion webhooks have persisted bounded attempts and a DNS-pinned,
  redirect-rejecting SSRF-hardened transport;
- CLI, authenticated API, OpenAPI, generated Python/TypeScript clients, Mintlify docs and ADR are
  synchronized;
- race, PostgreSQL, Docker stack, failure-recovery, Kind, automation-client, packaging, dead-code
  and vulnerability gates pass.

Commands:

- `make context`
- `make generate-api-check`
- `make test-container`
- `make dev-check`
- `make dev-check-full`
- `make qualify-contracts`
- `make qualify-local`

Durable local evidence:

- `.infercrane/dev-check/20260811T144000Z-49599`
- `.infercrane/contract-qualification/0f581567ce1e4a9062b9e879afd0411043cfc30d/qualification.json`
- `.infercrane/qualification/0f581567ce1e4a9062b9e879afd0411043cfc30d/qualification.json`

Real provider/runtime, GPU, performance and webhook-destination qualification is **DEFERRED** until
the consolidated manual workflow. No paid resource was created, no remote tag was pushed and no
package was published.
