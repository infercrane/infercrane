# v1.7.0-rc.1 evidence

Implementation commit: `9a4af9c8358bb3d3fe2b685147ee852f91f20021`

## Locally proven

- Recipe construction rejects mutable/missing artifacts, unversioned runtimes, and benchmark evidence
  that does not match the active revision and model identity.
- Canonical payload plus provenance produces a stable SHA-256 digest; PostgreSQL makes name/version
  idempotent for identical content and immutable for different content.
- Recipe search, lookup, and Lab evidence are tenant-scoped and bounded.
- Inference Lab compares only persisted AIPerf rows for the exact model and optional workload digest,
  labels every result `measured`, orders deterministically, and leaves missing metrics/cost unavailable.
- CLI, API, Python SDK, TypeScript SDK, OpenAPI, Mintlify, and generated repository context agree.
- `make dev-check-full` passed all historical race, migration, Kind, Docker, fault, HA, restore,
  production configuration, SDK, and documentation gates.
- `make qualify-rc` passed from a clean tree and wrote
  `.infercrane/qualification/9a4af9c8358bb3d3fe2b685147ee852f91f20021/qualification.json`.
- `make qualify-contracts` passed and wrote
  `.infercrane/contract-qualification/9a4af9c8358bb3d3fe2b685147ee852f91f20021/qualification.json`.

## Regression found and fixed

The first full development run exposed that both HA fixture replicas used the development bootstrap
entrypoint and raced to register identical targets. The second replica now runs `infercrane serve`
directly; the dedicated HA drill and complete suite passed afterward. This removes test-fixture
single-initializer coupling without weakening target conflict semantics.

## Deferred real evidence

No paid infrastructure was created. Real GPU AIPerf measurements, a public signed recipe registry,
cross-provider candidate orchestration, and modeled/heuristic evidence adapters remain deferred.
Local fixtures never appear as real-provider performance or cost claims.

