# v1.6.0-rc.1 evidence

Implementation commit: `44b842b9a8b3d231e0063cb320adc3a5d7e10cd7`

## Locally proven

- `make dev-check-full` passed race/vet/repository verification, provider/runtime contracts,
  Kubernetes Kind, automation clients, container integration, stack smoke, fault recovery,
  production configuration, and Mintlify checks.
- Two isolated control-plane replicas registered against one PostgreSQL database; after one stopped,
  the survivor remained ready and membership converged.
- Store tests proved compatible N/N+1 protocol overlap, disjoint live-version rejection, expired
  membership exclusion, operation lease recovery, and stale fence rejection.
- TLS tests proved TLS 1.3 server identity, mandatory client certificate rejection, valid client
  acceptance, custom CA support, and fail-closed partial configuration.
- Backup/restore safety fixtures and a real PostgreSQL 17 Docker drill proved archive verification,
  explicit target/approval guards, live-member refusal, migration-ledger preservation, and restored
  control-plane startup.
- `make qualify-rc` passed from a clean tree and wrote
  `.infercrane/qualification/44b842b9a8b3d231e0063cb320adc3a5d7e10cd7/qualification.json`.
- `make qualify-contracts` passed and wrote
  `.infercrane/contract-qualification/44b842b9a8b3d231e0063cb320adc3a5d7e10cd7/qualification.json`.

## Qualification incident

The first clean-tree attempt failed because Docker Desktop became unresponsive after the host data
volume reached 100% usage; the integration container observed a read-only `/tmp`. Docker Desktop was
restarted and only rebuildable Docker build cache was pruned. The preserved retry then passed against
the identical commit with 32+ GiB free. No gate was skipped or weakened.

## Deferred real evidence

No paid infrastructure was created. Customer PostgreSQL HA/failover, customer RTO/RPO, private
network topology, production certificate authority integration, and real provider/runtime disruption
qualification remain `DEFERRED`. Hermetic elastic/serverless stages are simulations and are not
described as cloud evidence.

