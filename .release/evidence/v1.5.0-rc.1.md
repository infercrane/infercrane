# v1.5.0-rc.1 evidence

Implementation commit: `1844c39be1f3ca220939d3b8b7a0bab44de94ed7`

## Locally proven

- `make dev-check-full` passed repository verification, Provider/Runtime Contract tests,
  acceptance safety, Kubernetes manifest and Kind recovery, Python/TypeScript automation clients,
  containerized race tests and vet, Compose stack smoke, failure recovery, production configuration,
  and Mintlify validation.
- `make qualify-rc` passed from a clean tree and wrote
  `.infercrane/qualification/1844c39be1f3ca220939d3b8b7a0bab44de94ed7/qualification.json`.
- `make qualify-contracts` passed and wrote
  `.infercrane/contract-qualification/1844c39be1f3ca220939d3b8b7a0bab44de94ed7/qualification.json`.
- The adapter identity is preserved by DeploymentSpec, CLI deploy/rollout, control API, immutable
  revision JSON, workflow selection, serverless fail-closed checks, and both public SDKs.
- Registration accepts multiple adapters for a cloud/runtime only with an unambiguous or explicit
  default; exact selection and restart replay resolve the persisted adapter.
- The hermetic GCP Compute adapter proves deterministic adoption after lost create response, private
  networking, attached service-account identity, immutable VM/container inputs, safe workload argv,
  readiness observation, owned inventory, and idempotent deletion.
- AWS ASG/EKS/SageMaker/Bedrock, GCP MIG/GKE/Vertex, and CoreWeave CKS profiles have independent
  ownership and capability boundaries. Their executable and real qualification states remain
  registered/deferred rather than fabricated.
- OpenAPI, Python, TypeScript, examples, Mintlify navigation, ADR, support matrix, project status,
  and generated repository context passed drift checks.

## Deferred real evidence

No paid infrastructure was created. Real AWS EC2/ASG/EKS/SageMaker/Bedrock, GCP Compute/MIG/GKE/
Vertex, CoreWeave CKS, RunPod, Kubernetes GPU, vLLM GPU, SGLang GPU, and custom OCI GPU qualification
remain `DEFERRED` for the final approval-locked consolidated workflow. No provider performance,
availability, or pricing claim is made from hermetic evidence.

## Qualification boundary

`local-qualified` proves deterministic adapter behavior against controlled provider/API fixtures and
local Docker environments. A registered profile proves only that its ownership and capability
boundary is explicit and inspectable. Neither state is evidence of real cloud capacity or runtime
compatibility.
