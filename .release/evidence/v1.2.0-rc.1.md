# v1.2.0-rc.1 automated evidence

Implementation commit: `c615ff4eb0af11a23f919d052754cd67ec96f921`

Passed on the exact clean implementation commit:

- atomic, tenant-isolated and idempotent adoption of existing workloads with an explicit
  `observe-only` to `traffic-managed` promotion boundary;
- fail-closed observe-only routing and traffic-managed endpoint compilation from persisted state
  into the in-memory request directory without request-path database reads;
- content-free request reconstruction with endpoint, binding, target, timing, token, retry, fallback,
  status and error identity;
- deterministic `Evidence → Rule → Finding` diagnosis with persisted evidence digests;
- canonical HMAC-signed webhooks with secret references, SSRF defenses, bounded retry, delivery
  idempotency and redacted errors;
- live Docker Compose adoption, request inspection, diagnosis, ownership promotion, external-target
  survival after endpoint deletion, worker-loss recovery, and control-plane restart recovery;
- the complete race-enabled Go suite against fresh PostgreSQL migration `028`, generated OpenAPI,
  Python and TypeScript clients, Kind, container tests, production configuration and packaging;
- Mintlify validation, link/anchor/snippet validation, and accessibility checks for 117 MDX files;
- dead-code and vulnerability audits plus the release-package gate.

Commands:

- `make context`
- `make verify`
- `make docs-check`
- `make dev-check-full`
- `make qualify-contracts`
- `make qualify-local`

Durable local evidence:

- `.infercrane/dev-check/20260811T133802Z-35861`
- `.infercrane/contract-qualification/c615ff4eb0af11a23f919d052754cd67ec96f921/qualification.json`
- `.infercrane/qualification/c615ff4eb0af11a23f919d052754cd67ec96f921/qualification.json`

Real external workload, AWS, GCP, CoreWeave, RunPod, Kubernetes GPU, vLLM GPU, SGLang GPU,
custom OCI GPU, performance, pricing, cancellation, and provider cleanup evidence is **DEFERRED**
until the single consolidated manual qualification. No paid resource was created, no remote tag was
pushed, and no package was published.
