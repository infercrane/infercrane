# v1.3.0-rc.1 automated evidence

Implementation commit: `3c4be1139e231edd5198b1337b066276cf8e2dfa`

Passed on the exact clean implementation commit:

- immutable route snapshots independently gate Chat Completions, Completions, Embeddings,
  Responses, online chat batch, streaming and tool-calling capabilities;
- unknown or unsupported protocol claims fail before upstream network transmission;
- qualified protocol requests preserve native fields and responses while rewriting only the stable
  logical model identity and controlled upstream authentication;
- protocol operation identity and both prompt/completion and input/output token usage normalize
  into content-free request evidence;
- `infercrane request --protocol` and `/v1/models` capability discovery expose the same behavior;
- the pinned vLLM `0.8.5.post1` profile honestly qualifies Chat, Completions and model-dependent
  Embeddings while refusing to claim newer Responses and online batch support;
- live Docker Compose routes Completions and Embeddings through the supervised router, alongside all
  earlier chat, adoption, diagnostics, failure-recovery and restart checks;
- OpenAPI, generated Python/TypeScript clients, Kind, container, packaging, dead-code,
  vulnerability and 119-page Mintlify gates pass.

Commands:

- `make context`
- `make verify`
- `make docs-check`
- `make dev-check-full`
- `make qualify-contracts`
- `make qualify-local`

Durable local evidence:

- `.infercrane/dev-check/20260811T135858Z-41663`
- `.infercrane/contract-qualification/3c4be1139e231edd5198b1337b066276cf8e2dfa/qualification.json`
- `.infercrane/qualification/3c4be1139e231edd5198b1337b066276cf8e2dfa/qualification.json`

Real runtime protocol, model-task, GPU, provider, performance and cleanup evidence is **DEFERRED**
until the consolidated manual qualification. No paid resource was created, no remote tag was
pushed, and no package was published.
