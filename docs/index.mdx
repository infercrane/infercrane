---
title: Production inference without the platform engineering.
description: A durable, extensible control plane for production inference on infrastructure you control.
sidebarTitle: Introduction
mode: wide
---

<div className="ic-hero not-prose">
  <div className="ic-kicker"><span className="ic-status-dot"></span> v0.2 release candidate development</div>
  <div className="ic-actions">
    <a className="ic-button ic-button-primary" href="/quickstart">Start the quickstart <span aria-hidden="true">→</span></a>
    <a className="ic-button ic-button-secondary" href="https://github.com/infercrane/infercrane">View on GitHub <span aria-hidden="true">↗</span></a>
  </div>
  <div className="ic-proof">
    <span>Adapter-based runtimes</span>
    <span>Provider-owned compute</span>
    <span>No Kubernetes required</span>
  </div>
</div>

<div className="ic-terminal not-prose" role="region" aria-label="InferCrane deployment command">
  <div className="ic-terminal-bar">
    <div className="ic-terminal-lights" aria-hidden="true"><span></span><span></span><span></span></div>
    <span>deploy · qwen3-8b</span>
    <span className="ic-terminal-state"><i></i> durable operation</span>
  </div>
  <div className="ic-terminal-body"><span className="ic-prompt">$</span> infercrane deploy <span className="ic-code-value">Qwen/Qwen3-8B</span><br/><span className="ic-code-muted"># submits one durable operation through the control-plane API</span><br/><span className="ic-code-muted"># disconnecting the CLI does not abandon provisioning</span></div>
</div>

```bash
infercrane request qwen3-8b --message "Hello" --stream
```

InferCrane gives applications one OpenAI-compatible logical endpoint while durable control-plane operations provision, reconcile, scale, and safely replace inference workers behind it. Providers and runtimes integrate through explicit contracts; Kubernetes is not required.

<div className="ic-flow not-prose" aria-label="InferCrane request path">
  <div className="ic-node"><span className="ic-node-label">request</span><strong>OpenAI client</strong></div>
  <span className="ic-flow-arrow" aria-hidden="true">→</span>
  <div className="ic-node ic-node-accent"><span className="ic-node-label">stable alias</span><strong>deployment</strong></div>
  <span className="ic-flow-arrow" aria-hidden="true">→</span>
  <div className="ic-node"><span className="ic-node-label">policy</span><strong>route generation</strong></div>
  <span className="ic-flow-arrow" aria-hidden="true">→</span>
  <div className="ic-node"><span className="ic-node-label"><i className="ic-dot"></i> healthy</span><strong>runtime replicas</strong></div>
</div>

## The operational layer around inference runtimes

<CardGroup cols={3}>
  <Card title="Deploy" icon="server" href="/features/lifecycle">
    Create a durable deployment through a qualified provider backend or connect existing workers. Disconnecting the CLI does not abandon the operation.
  </Card>
  <Card title="Protect releases" icon="shield-check" href="/features/release-guard">
    Compare active and candidate revisions with deterministic, persisted Release Guard policy.
  </Card>
  <Card title="Understand behavior" icon="wave-pulse" href="/features/explanations">
    Explain scaling, rollout, degradation, and cold starts from recorded state and measurements.
  </Card>
</CardGroup>

## One product, two compute modes

<Tabs>
  <Tab title="Elastic">
    A registered elastic backend provisions runtime replicas. InferCrane reconciles desired capacity, routes only to ready workers, and fences scale-down before provider termination.

    ```bash
    infercrane deploy Qwen/Qwen3-8B \
      --cloud runpod \
      --gpu L40S \
      --min 1 \
      --max 4
    ```
  </Tab>
  <Tab title="Serverless">
    A provider-native serverless backend owns worker allocation and scale-to-zero. InferCrane preserves the logical endpoint, lifecycle evidence, cancellation, and cleanup.

    ```bash
    infercrane deploy Qwen/Qwen3-8B \
      --compute serverless \
      --cloud runpod \
      --gpu L40S \
      --max 4
    ```
  </Tab>
</Tabs>

<Note>
InferCrane is provider- and runtime-extensible by design. The first v0.1 qualification target is vLLM on RunPod; other integrations are not yet qualified. Consult [capability status](/project-status) before relying on experimental paths.
</Note>

## Evidence, not guesses

InferCrane records durable operation events, immutable model and revision identity, normalized request telemetry, benchmark reproduction metadata, and deterministic policy decisions. It does not fabricate provider pricing or timing boundaries that the provider does not expose.

<CardGroup cols={2}>
  <Card title="Five-minute local tour" icon="rocket" href="/quickstart">
    Run the GPU-free development stack and send your first OpenAI-compatible request.
  </Card>
  <Card title="Read the architecture" icon="diagram-project" href="/architecture/system">
    See the control-plane and data-plane boundaries behind durable operations.
  </Card>
</CardGroup>
